# vmlab

VM lab tooling — fresh rewrite in Rust.

## Status

Early scaffold — see `docs/PRD.md` for the product requirements (pending).

Previous iterations are archived in the private graveyard
(`vmlab`, `vmlab-2`, `vmlab_rust`, `vmlab_qemu`, `vmlab_oci`,
`vmlab_floppy`, and many more).

## Building

`just` is the command runner: `just build`, `just test`, `just check`.
